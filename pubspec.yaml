name: protube_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  # 🌟 THE NEW OFFICIAL YOUTUBE WEB PLAYER 🌟
  youtube_player_iframe: ^3.1.2
  
  # 🌟 तुम्हारे प्रोजेक्ट के बाकी सभी जरूरी पैकेजेस 🌟
  youtube_explode_dart: ^2.2.3
  shared_preferences: ^2.2.3 
  http: ^1.2.0
  share_plus: ^7.2.2
  flutter_volume_controller: ^2.0.1
  screen_brightness: ^2.1.1
  just_audio: ^0.9.36

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
