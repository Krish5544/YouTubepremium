name: protube_app
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.1
  
  # 🌟 यहाँ हमने वर्जन फिक्स कर दिए हैं ताकि कोई एरर न आए 🌟
  youtube_player_flutter: ^8.1.2
  youtube_explode_dart: ^2.5.3
  just_audio: ^0.9.36
  audio_session: ^0.1.16

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
