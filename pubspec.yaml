name: protube_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  youtube_player_flutter: ^9.0.0
  youtube_explode_dart: ^2.0.0
  shared_preferences: ^2.2.3 
  http: ^1.2.0
  share_plus: ^7.2.2
  
  # 🌟 तुम्हारे कहे अनुसार एकदम लेटेस्ट और मॉडर्न पैकेजेस 🌟
  flutter_volume_controller: ^2.0.1
  screen_brightness: ^2.1.1
  
  # 🎤 माइक (Voice Search) के लिए नया पैकेज 🌟
  speech_to_text: ^6.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
